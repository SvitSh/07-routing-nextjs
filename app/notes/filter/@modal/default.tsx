/**
 * Default component for the `@modal` slot.  When no modal route is
 * matched, this component renders nothing.  Leaving this file in
 * place prevents Next.js from throwing an error about a missing
 * default parallel route.
 */
export default function ModalDefault() {
  return null;
}